@model IEnumerable<Lands.Domain.GetServices.CantvData>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { }, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodePhone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberPhone)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodePhone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberPhone)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CantvDataId }, new { @class = "btn btn-warning" })
                @Html.ActionLink("Details", "Details", new { id = item.CantvDataId }, new { @class = "btn btn-info" })
                @Html.ActionLink("Delete", "Delete", new { id = item.CantvDataId }, new { @class = "btn btn-danger" })
            </td>

        </tr>
    }

</table>